#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 *_strlen_recursion - length of a string
 *@s: the string
 *Return: Always o
 */
int _strlen_recursion(char *s)
{
	int length = strlen(s);

	return (length);
}
