#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 *_strcmp - compare strings
 *@s1: string one
 *@s2: string two
 *Return: Always 0
 */
int _strcmp(char *s1, char *s2)
{
	int ret;

	ret = strcmp(s1, s2);

	return (ret);
}
