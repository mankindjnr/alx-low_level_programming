#include <string.h>
#include <ctype.h>
#include "main.h"
/**
 *_strlen - the length of a string
 *@s: the string
 *Return: Always 0 - success
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
