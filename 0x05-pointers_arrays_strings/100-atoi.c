#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 *_atoi - convert strings to ints
 *@s: the string to convert
 *Return: Always 0
 */
int _atoi(char *s)
{
	int converted;

	converted = atoi(s);

	return (converted);
}
